# `equilibria.sam_tools`

```{eval-rst}
.. automodule:: equilibria.sam_tools.api
   :members:
```

## Transformation primitives

```{eval-rst}
.. automodule:: equilibria.sam_tools.mip_to_sam_transforms
   :members:
   :exclude-members: create_government_flows, create_investment_account
```

```{note}
`create_government_flows` and `create_investment_account` are excluded from
the generated reference until their docstrings are valid reST (unbalanced
`*` emphasis breaks the strict docs build).
```

## Models

```{eval-rst}
.. automodule:: equilibria.sam_tools.models
   :members:
```
