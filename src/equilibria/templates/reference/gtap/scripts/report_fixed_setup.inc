* -------------------------------------------------------------------------
*  Report fixed/free counts by variable - setup (declare symbols/macros).
*  Include once before the simulation loop.
* -------------------------------------------------------------------------

$onMultiR

parameter
    fixed_count(*,*)
    free_count(*,*)
    total_count(*,*)
;

* Macro definitions are in report_fixed_run.inc
